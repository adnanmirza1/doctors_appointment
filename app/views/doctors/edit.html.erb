<h1>Editing Post</h1>
<div class="form-container">
  <h1>Add Pet Details(Add Doctor)</h1>
  <%= form_with(model: @doctor, local: true, class: "ui form" ) do |f| %>
    <div class="field">
      <%= f.label :name%>
      <%= f.text_field :name, required: true, placeholder: "Pet Name" %>
    </div>
    <div class="field">
      <%= f.label :age %>
      <%= f.text_field :age, required: true, placeholder: "age" %>
    </div>
    <div class="field">
      <%= f.label :MedicalHistory %>
      <%= f.text_field :medicalhistory, required: true, placeholder: "Medical History" %>
    </div>

    <%= f.submit 'Update Record', class: "ui purple button inverted" %>
  <% end %>
</div>
