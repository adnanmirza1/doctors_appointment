<nav>
    <div class="navigation">
        <% if user_signed_in? %>
        <div class="nav-links">
          <%= link_to "Home", root_path %>
            <%= link_to "Pet Detail Records (Doctors)", doctors_path %>
            <%= link_to 'Pet owner Profile(Patients)', patients_path %>
            <%= link_to 'Add Pet owner Profile(Add Patient)', new_patient_path %>
        </div>
        <% end %>
        <% if user_signed_in? %>
        <div class="user-login">
            <%= "Welcome #{current_user.full_name}" %>
            <%= link_to "Log Out", destroy_user_session_path, method: :delete, class:"logout"%>
        </div>
        <% end %>

    </div>
</nav>
